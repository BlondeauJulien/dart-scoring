const inputsValues = {
  gameTypeValues: [ 101, 301, 501, 701, 1001 ],
  setOptionsValues: Array.from(Array(11), (x, i) => i+1),
  legOptionsValues: Array.from(Array(11), (x, i) => i+1),
  numberOfPlayers: Array.from(Array(4), (x, i) => i+1)
};

export default inputsValues;